all:
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: keys/id_rsa
  hosts:
    anise:
    basil:
    cardamom:
    dill:
    pikvm:

rack:
  hosts:
    anise:
    basil:
    cardamom:
    pikvm:

networking:
  hosts:
    dill:

proxmox:
  hosts:
    anise:
    basil:
    cardamom:
    dill:
