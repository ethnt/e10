---
- name: Import tasks
  ansible.builtin.include_tasks: "{{ task_file }}"
  loop:
    - dnsmasq.yml
    - git.yml
    - qemu.yml
    - mdns.yml
    - prometheus-node-exporter.yml
    - ddclient.yml
    - acme.yml
  loop_control:
    loop_var: task_file
